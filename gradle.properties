org.gradle.jvmargs=-Xmx2g
kotlin.code.style=official
# The separate Kotlin compile daemon process crashes on startup in Termux
# (jansi's native lib needs glibc, which Android/Bionic doesn't provide).
# Running the compiler in-process inside the Gradle JVM sidesteps it.
kotlin.compiler.execution.strategy=in-process
